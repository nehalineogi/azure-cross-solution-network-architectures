trigger: 
  batch: true
  branches:
    include:
      - aks-private

  paths:
    include:
      - bicep/aks/main.bicep
      - bicep/aks/modules/*
      
jobs:
  - job:
    steps:

    - task: AzureCLI@2
      inputs:
        azureSubscription: $(ServiceConnectionName)
        scriptType: 'bash'
        scriptLocation: 'inlineScript'
        inlineScript: |
          az deployment sub create \
            --name $(Build.BuildNumber)-public-kubenet \
            --location $(location) \
            --template-file bicep/aks/main.bicep \
            --parameters adUserId=$(adUserId) ResourceGroupName=$(ResourceGroupName_pbk) networkPlugin=$(NetworkPlugin_kubenet) aksPrivatePublic=$(aksPrivatePublic_pbl)

  - job:
    steps:
      
    - task: AzureCLI@2
      inputs:
        azureSubscription: $(ServiceConnectionName)
        
        scriptType: 'bash'
        scriptLocation: 'inlineScript'
        inlineScript: |
          az deployment sub create \
            --name $(Build.BuildNumber)-private-kubenet \
            --location $(location) \
            --template-file bicep/aks/main.bicep \
            --parameters adUserId=$(adUserId) ResourceGroupName=$(ResourceGroupName_prv) networkPlugin=$(NetworkPlugin_kubenet) aksPrivatePublic=$(aksPrivatePublic_prv)

  - job:
    steps:

    - task: AzureCLI@2
      inputs:
        azureSubscription: $(ServiceConnectionName)
        scriptType: 'bash'
        scriptLocation: 'inlineScript'
        inlineScript: |
          az deployment sub create \
            --name $(Build.BuildNumber)-public-cni \
            --location $(location) \
            --template-file bicep/aks/main.bicep \
            --parameters adUserId=$(adUserId) ResourceGroupName=$(ResourceGroupName_cpb) networkPlugin=$(NetworkPlugin_CNI) aksPrivatePublic=$(aksPrivatePublic_pbl)

  - job:
    steps:

    - task: AzureCLI@2
      inputs:
        azureSubscription: $(ServiceConnectionName)
        scriptType: 'bash'
        scriptLocation: 'inlineScript'
        inlineScript: |
          az deployment sub create \
            --name $(Build.BuildNumber)-private-cni \
            --location $(location) \
            --template-file bicep/aks/main.bicep \
            --parameters adUserId=$(adUserId) ResourceGroupName=$(ResourceGroupName_cpr) networkPlugin=$(NetworkPlugin_CNI) aksPrivatePublic=$(aksPrivatePublic_prv)